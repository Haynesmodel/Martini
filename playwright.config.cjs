const { defineConfig, devices } = require('@playwright/test');
module.exports = defineConfig({ testDir: 'test', testMatch: '**/*.spec.cjs', webServer: { command: 'npm run build:preview', port: 4173, reuseExistingServer: false, timeout: 120000 }, use: { baseURL: `http://127.0.0.1:4173${process.env.PLAYWRIGHT_BASE_PATH || '/Martini/'}` }, projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }, { name: 'webkit', use: { ...devices['Desktop Safari'] } }] });
